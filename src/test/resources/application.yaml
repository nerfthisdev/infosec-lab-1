spring:
  jpa:
    hibernate:
      ddl-auto: create-drop

security:
  jwt:
    # Deterministic test-only key; production reads JWT_SECRET from the environment.
    secret: MDEyMzQ1Njc4OTAxMjM0NTY3ODkwMTIzNDU2Nzg5MDE=
